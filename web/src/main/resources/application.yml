server:
  port: 8081
  servlet:
    context-path: /

spring:
  application:
    name: web

gate:
  host: 127.0.0.1
  port: 5600

lobby:
  admin-http: http://127.0.0.1:5701

game:
  # 相对 web 启动目录；若从 repos/Server 起服可用此路径
  replay-dir: build/game/replay

# 日志级别与滚动见 logback-spring.xml（勿在此写 logging.level.web，会命中 Spring Boot web 日志组）
