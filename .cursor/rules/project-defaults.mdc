---
description: 项目默认协作与代码习惯（可按需扩充；对齐常见阿里巴巴 Java 规范要点）
alwaysApply: true
---

# 默认协作规则

回答与改代码时遵守下列约定；后续可在本文件中追加或细化条目。

## 通用

- **工作空间**：可读写的目录仅限当前 Cursor 工作空间（本仓库根目录及其子目录）；不要假设或依赖工作空间外的路径，除非用户明确要求。
- 先理解需求再动手；改动范围尽量小，不做无关重构。
- 说明变更时用清晰句子；引用现有代码用 Cursor 要求的代码引用格式。

## Java（阿里巴巴规范常见要点 — 可继续补充）

- **命名**：类/接口用大驼峰 `UserService`；方法与变量用小驼峰 `findById`；常量全大写下划线 `MAX_RETRY`。
- **格式**：左花括号不换行（K&R）；`if/for` 等始终带花括号，即使只有一行。
- **空与安全**：集合判空用 `CollectionUtils.isEmpty` 或 `isEmpty()`，避免裸 `size() == 0`；字符串比较用 `Objects.equals(a, b)` 或常量在前 `"x".equals(var)`。
- **并发**：共享可变状态要有清晰的所有权与同步策略；避免在锁内做耗时或可能嵌套加锁的事。
- **异常**：不要吞掉异常；业务含义用受检/非受检与项目既有风格一致；日志带上下文。
- **魔法值**：业务含义的数字、字符串提成常量或枚举，避免散落字面量。

## 协议 / Proto（`proto` 工程）

- 工作空间内的 **`proto`** 指本仓库下的 `proto/` 模块（`.proto` 及与之相关的协议改动）。
- 若在会话中**修改了该模块下的协议或会影响生成的文件**，改完后应**立即执行生成脚本**：先 `cd` 到 `proto/src/main/java`（脚本内使用 `./xxx.proto` 相对路径，必须从该目录运行），再运行 `genProto.bat`。工作空间内完整路径为：`proto/src/main/java/genProto.bat`。

## 你可自行追加

在下面按项目实际情况增加小节即可（例如包结构、日志框架、禁止的 API、数据库约定等）。
